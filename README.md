# struct_light
This is a library of 3D algorithms based on structured light vision
The library mainly contains phase-shift and Gray code encoding and decoding operations, binocular camera calibration, parcel phase calculation, parcel phase settlement, binocular stereo matching and three-bit point cloud data generation, etc. The library is still in the process of being updated.
